import classNames from "classnames"

const MapComponent: React.FC = () => {
  return (
    <div className={classNames([`w-[inherit]`, `h-[inherit]`])}>
      <h1>MAP</h1>
    </div>
  )
}

export default MapComponent
